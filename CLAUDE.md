# CLAUDE.md

> **如果你是 Claude Code，这份就是给你的。**
> Codex 读的是 [AGENTS.md](AGENTS.md)，内容就是下面指向的那一份——两边同一套规矩。

这是 **MotionControl 手机端**，由 **Claude Code** 和 **Codex** 共同开发，人类负责人
是 guiwu（GitHub: guiwushengzhe-prog）。

**你的实验分支叫 `exp/claude/描述`。** Codex 用 `exp/codex/描述`，别用错——
分支名就是用来看出这是谁开的。`main` 只放已经发出去的版本。

手机端特有的约定（两个版本号不要混、versionCode 不要手填、新 APK 要丢弃旧热更包）
在 **[AGENTS.md](AGENTS.md)**。完整约定在电脑端仓库：
https://github.com/guiwushengzhe-prog/MotionControl/blob/main/AGENTS.md
