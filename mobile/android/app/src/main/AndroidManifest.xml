<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- allowBackup is off: the server address and device id cost nothing to
         re-enter.  usesCleartextTraffic stays on because the phone talks to
         the PC over ws:// on the LAN. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Android 11+ hides other packages unless they are declared here. Without this,
         resolveActivity() on the settings app comes back null and the "open the tethering
         page for me" button silently falls all the way through to the wireless settings
         page on every device. -->
    <queries>
        <package android:name="com.android.settings" />
    </queries>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Discovery sends UDP broadcast and reads unicast replies, and INTERNET
         already covers both of those. This is for noticing that the network
         changed (the cable came out, the hotspot moved to another subnet), so
         the app can look again instead of waiting for the player to.
         Declared now rather than when that code lands: it is a normal
         permission with no prompt, and adding one costs a new APK, which is the
         exact thing the hot-update channel exists to avoid. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-feature android:name="android.hardware.camera.any" android:required="true" />
</manifest>
